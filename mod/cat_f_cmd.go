package mod

func cmnd_cat_file(object string, typeObj string, args ...string) {
	// repo = repo_find()
	// cmnd_cat_file(repo, object, typeObj)
}

func cat_file(repo GitRepository, obj GitObject, header string) {
	// obj = object_read(repo,object_find(repo,obj,header))
	// obj.serialize()
}
